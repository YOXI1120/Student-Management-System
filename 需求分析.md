# 学生管理系统（大学版）- 完整需求分析与数据库设计方案

## 一、需求分析

### 1.1 系统目标
构建一个基于 Vue 3 + Element Plus（前端）和 Node.js Express（后端）的大学学生管理系统，使用 Supabase (PostgreSQL) 作为数据库，实现学院、班级、学生、课程、选课、成绩的信息化管理，支持多角色权限控制及数据导入导出。

### 1.2 角色与权限

| 角色 | 权限范围 |
|------|----------|
| 管理员 (admin) | 所有功能的完全访问权限，包括系统管理、角色分配、发布公告 |
| 教师 (teacher) | 查看学院/班级/学生、录入/修改成绩、导出数据、查看课程安排 |
| 学生 (student) | 查看个人信息、选课（选修课）、查看本人成绩、查看课表 |

### 1.3 功能模块

#### 模块一：学院管理
- 学院信息增删改查（学院名称、学院代码、院长、联系电话）

#### 模块二：班级管理
- 班级信息增删改查（班级名称、年级、所属学院、班主任）
- 查看班级学生列表

#### 模块三：学生管理
- 学生信息增删改查（学号、姓名、性别、身份证号、出生日期、电话、邮箱、住址、入学日期、所属班级、状态）
- **学号规则**：8 位数字，前 4 位入学年份，后 4 位按姓名首字母顺序分配
- **学籍状态管理**：支持休学、复学、退学、毕业等状态变更，记录变更历史（含操作人、时间、原因）
- 数据导出：筛选后导出学生名单（Excel）

#### 模块四：教师管理
- 教师信息增删改查（教师编号、姓名、性别、身份证号、电话、邮箱、所属学院）

#### 模块五：课程管理
- 课程信息增删改查（课程名称、课程代码、学分、学时、课程类型、开课学院、课程描述）
- **课程类型**：必修课 / 选修课

#### 模块六：排课管理
- 排课时指定课程、教师、班级、学期、上课时间（星期几 + 第几节）
- **冲突检测**：同一教师/同一班级在同一时间不能安排多门课程
- 必修课自动关联到学生选课记录，选修课由学生自行选择

#### 模块七：选课管理
- **必修课**：排课后自动为对应班级学生生成选课记录
- **选修课**：学生在可选课程列表中自行选课，需检测时间冲突
- 学生可查看已选课程，退选选修课

#### 模块八：成绩管理
- 成绩录入（教师端）：选择课程 → 班级 → 批量录入/修改成绩
- 成绩查询：按学生/课程/班级维度查询
- 成绩统计：平均分、最高分/最低分、及格率
- 数据导出：筛选后导出成绩单（Excel）

#### 模块九：系统管理
- 用户管理：创建/管理系统登录账户，分配角色
- 个人中心：修改密码、查看个人信息

#### 模块十：公告管理
- 管理员发布、编辑、删除公告
- 所有用户在首页可查看最新公告

---

## 二、数据库表结构设计

### 2.1 表关系总览

```
colleges (1) ──→ (N) classes                     # 学院包含多个班级
colleges (1) ──→ (N) courses                     # 学院开设课程
colleges (1) ──→ (N) teachers                    # 教师属于学院
classes (1) ──→ (N) students                     # 班级含多名学生
teachers (1) ──→ (N) classes (head_teacher)      # 班主任
teachers (1) ──→ (N) course_schedules            # 教师排课
courses (1) ──→ (N) course_schedules             # 课程排课
classes (1) ──→ (N) course_schedules             # 班级排课
course_schedules (1) ──→ (N) student_courses     # 排课下的选课
students (1) ──→ (N) student_courses             # 学生选课
student_courses (1) ──→ (N) scores               # 选课对应的成绩
students (1) ──→ (N) student_status_logs         # 学籍变更记录
users (1) ──→ (N) announcements                  # 管理员发布公告
users (1) ──→ (1) students                       # 用户关联学生
users (1) ──→ (1) teachers                       # 用户关联教师
```

### 2.2 colleges（学院表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK, DEFAULT gen_random_uuid() | 主键 |
| name | VARCHAR(100) | NOT NULL | 学院名称 |
| code | VARCHAR(20) | UNIQUE, NOT NULL | 学院代码 |
| dean | VARCHAR(50) | | 院长 |
| phone | VARCHAR(20) | | 联系电话 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

### 2.3 users（用户表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK, DEFAULT gen_random_uuid() | 主键 |
| username | VARCHAR(50) | UNIQUE, NOT NULL | 用户名 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希 |
| role | VARCHAR(20) | NOT NULL, DEFAULT 'student' | 角色: admin/teacher/student |
| status | VARCHAR(20) | NOT NULL, DEFAULT 'active' | 状态: active/disabled |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

### 2.4 students（学生信息表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK, DEFAULT gen_random_uuid() | 主键 |
| user_id | UUID | FK → users.id, UNIQUE, NULL | 关联用户ID |
| student_no | VARCHAR(8) | UNIQUE, NOT NULL | 学号（8位数字） |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| id_card | VARCHAR(18) | UNIQUE, NOT NULL | 身份证号（18位） |
| gender | VARCHAR(10) | | 性别 |
| date_of_birth | DATE | | 出生日期 |
| phone | VARCHAR(20) | | 电话 |
| email | VARCHAR(100) | | 邮箱 |
| address | VARCHAR(255) | | 住址 |
| enrollment_date | DATE | | 入学日期 |
| class_id | UUID | FK → classes.id, NULL | 所属班级 |
| status | VARCHAR(20) | DEFAULT 'active' | 在校状态: active/graduated/suspended/dropped |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

> **学号生成规则**：8 位数字，前 4 位 = 入学年份（如 2024），后 4 位按学生姓名拼音首字母在同年入学学生中排序后顺序分配（0001-9999）。

### 2.5 teachers（教师信息表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| user_id | UUID | FK → users.id, UNIQUE, NULL | 关联用户ID |
| teacher_no | VARCHAR(20) | UNIQUE, NOT NULL | 教师编号 |
| name | VARCHAR(50) | NOT NULL | 姓名 |
| id_card | VARCHAR(18) | UNIQUE, NOT NULL | 身份证号（18位） |
| gender | VARCHAR(10) | | 性别 |
| phone | VARCHAR(20) | | 电话 |
| email | VARCHAR(100) | | 邮箱 |
| college_id | UUID | FK → colleges.id, NULL | 所属学院 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

### 2.6 classes（班级表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| name | VARCHAR(100) | NOT NULL | 班级名称 |
| grade | VARCHAR(20) | | 年级（如: 2023） |
| college_id | UUID | FK → colleges.id, NOT NULL | 所属学院 |
| head_teacher_id | UUID | FK → teachers.id, NULL | 班主任 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

### 2.7 courses（课程表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| name | VARCHAR(100) | NOT NULL | 课程名称 |
| code | VARCHAR(20) | UNIQUE, NOT NULL | 课程代码 |
| credits | DECIMAL(3,1) | NOT NULL, DEFAULT 0 | 学分 |
| hours | INTEGER | DEFAULT 0 | 学时 |
| course_type | VARCHAR(20) | NOT NULL, DEFAULT 'required' | 课程类型: required(必修)/elective(选修) |
| college_id | UUID | FK → colleges.id, NOT NULL | 开课学院 |
| description | TEXT | | 课程描述 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

### 2.8 course_schedules（排课表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| course_id | UUID | FK → courses.id, NOT NULL | 课程ID |
| teacher_id | UUID | FK → teachers.id, NOT NULL | 授课教师ID |
| class_id | UUID | FK → classes.id, NOT NULL | 上课班级ID |
| semester | VARCHAR(20) | NOT NULL | 学期: spring/autumn |
| academic_year | VARCHAR(20) | NOT NULL | 学年（如: 2025-2026） |
| day_of_week | INTEGER | NOT NULL | 星期几（1=周一 ~ 7=周日） |
| start_period | INTEGER | NOT NULL | 开始节次（1~12） |
| end_period | INTEGER | NOT NULL | 结束节次 |
| location | VARCHAR(100) | | 上课地点（暂不强制） |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

> **冲突检测约束**：同一教师在同一学期、星期、节次不能安排两门课；同一班级同理。

### 2.9 student_courses（选课表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| student_id | UUID | FK → students.id, NOT NULL | 学生ID |
| course_schedule_id | UUID | FK → course_schedules.id, NOT NULL | 排课ID |
| is_auto | BOOLEAN | DEFAULT true | true=必修自动分配, false=选修自选 |
| status | VARCHAR(20) | DEFAULT 'enrolled' | 状态: enrolled/dropped/completed |
| enrolled_at | TIMESTAMP | DEFAULT NOW() | 选课时间 |
| dropped_at | TIMESTAMP | | 退选时间 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

> **唯一约束**：(student_id, course_schedule_id)
> **冲突检测**：学生选课前检查该学生已有课程中是否存在时间冲突

### 2.10 scores（成绩表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| student_course_id | UUID | FK → student_courses.id, NOT NULL | 选课ID |
| score | DECIMAL(5,2) | | 成绩分数 |
| exam_type | VARCHAR(20) | DEFAULT 'final' | 考试类型: regular/midterm/final/makeup |
| exam_date | DATE | | 考试日期 |
| remarks | VARCHAR(255) | | 备注 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

> **唯一约束**：(student_course_id, exam_type)

### 2.11 student_status_logs（学籍变更记录表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| student_id | UUID | FK → students.id, NOT NULL | 学生ID |
| from_status | VARCHAR(20) | | 变更前状态 |
| to_status | VARCHAR(20) | NOT NULL | 变更后状态 |
| reason | VARCHAR(500) | | 变更原因 |
| operator_id | UUID | FK → users.id | 操作人 |
| created_at | TIMESTAMP | DEFAULT NOW() | |

### 2.12 announcements（公告表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| id | UUID | PK | 主键 |
| title | VARCHAR(200) | NOT NULL | 标题 |
| content | TEXT | NOT NULL | 内容 |
| publisher_id | UUID | FK → users.id | 发布人 |
| is_published | BOOLEAN | DEFAULT false | 是否已发布 |
| published_at | TIMESTAMP | | 发布时间 |
| created_at | TIMESTAMP | DEFAULT NOW() | |
| updated_at | TIMESTAMP | DEFAULT NOW() | |

---

## 三、API 接口设计

### 认证模块
```
POST /api/auth/login              # 登录 → 返回 JWT token
GET  /api/auth/profile            # 获取当前用户信息
PUT  /api/auth/password           # 修改密码
```

### 学院管理
```
GET    /api/colleges              # 学院列表
POST   /api/colleges              # 新增学院
PUT    /api/colleges/:id          # 修改学院
DELETE /api/colleges/:id          # 删除学院
```

### 学生管理
```
GET    /api/students              # 学生列表（分页 + 筛选）
GET    /api/students/:id          # 学生详情
POST   /api/students              # 新增学生（自动生成学号）
PUT    /api/students/:id          # 修改学生
DELETE /api/students/:id          # 删除学生
```

### 教师管理
```
GET    /api/teachers              # 教师列表
GET    /api/teachers/:id          # 教师详情
POST   /api/teachers              # 新增教师
PUT    /api/teachers/:id          # 修改教师
DELETE /api/teachers/:id          # 删除教师
```

### 班级管理
```
GET    /api/classes               # 班级列表（含学院筛选）
GET    /api/classes/:id           # 班级详情（含学生列表）
POST   /api/classes               # 新增班级
PUT    /api/classes/:id           # 修改班级
DELETE /api/classes/:id           # 删除班级
```

### 课程管理
```
GET    /api/courses               # 课程列表
POST   /api/courses               # 新增课程
PUT    /api/courses/:id           # 修改课程
DELETE /api/courses/:id           # 删除课程
```

### 排课管理
```
GET    /api/schedules                     # 排课列表（含筛选）
POST   /api/schedules                     # 新增排课（含冲突检测）
DELETE /api/schedules/:id                 # 删除排课
GET    /api/schedules/by-class/:id        # 按班级查询排课
```

### 选课管理
```
GET    /api/student-courses               # 选课列表（学生查看自己的选课）
GET    /api/student-courses/available      # 学生查看可选选修课列表
POST   /api/student-courses/select         # 学生选修选修课（需冲突检测）
DELETE /api/student-courses/:id/drop       # 学生退选选修课
GET    /api/student-courses/schedule       # 学生查看课表
POST   /api/student-courses/auto-assign    # 管理员/教师触发必修课自动分配
```

### 成绩管理
```
GET    /api/scores                        # 成绩列表（可筛选）
POST   /api/scores/batch                  # 批量录入成绩
GET    /api/scores/student/:id            # 查询某学生成绩
GET    /api/scores/statistics             # 成绩统计
```

### 学籍管理
```
GET    /api/students/:id/status-logs      # 查看学籍变更记录
POST   /api/students/:id/change-status    # 变更学生状态
```

### 公告管理
```
GET    /api/announcements                 # 公告列表
GET    /api/announcements/latest          # 首页最新公告
POST   /api/announcements                 # 发布公告（管理员）
PUT    /api/announcements/:id             # 编辑公告（管理员）
DELETE /api/announcements/:id             # 删除公告（管理员）
```

### 用户管理（仅管理员）
```
GET    /api/users           # 用户列表
POST   /api/users           # 创建用户
PUT    /api/users/:id       # 修改用户
DELETE /api/users/:id       # 删除用户
```

### 数据导出
```
POST   /api/export/students    # 导出筛选后的学生名单（Excel）
POST   /api/export/scores      # 导出筛选后的成绩单（Excel）
```

---

## 四、前端路由设计

| 路径 | 组件 | 角色 | 说明 |
|------|------|------|------|
| /login | Login | 所有 | 登录页 |
| / | Layout/Dashboard | 所有 | 首页（含公告） |
| /colleges | CollegeList | admin | 学院管理 |
| /students | StudentList | admin, teacher | 学生列表 |
| /students/add | StudentForm | admin | 新增学生 |
| /students/:id/edit | StudentForm | admin | 编辑学生 |
| /students/:id/status-logs | StudentStatusLogs | admin | 学籍变更记录 |
| /teachers | TeacherList | admin | 教师列表 |
| /classes | ClassList | admin, teacher | 班级列表 |
| /classes/:id | ClassDetail | admin, teacher | 班级详情 |
| /courses | CourseList | admin, teacher | 课程列表 |
| /schedules | ScheduleList | admin, teacher | 排课管理 |
| /schedules/add | ScheduleForm | admin | 新增排课 |
| /courses/elective | ElectiveList | student | 选修课选课 |
| /my-courses | MyCourses | student | 我的课表/选课 |
| /scores | ScoreList | admin, teacher, student | 成绩查询 |
| /scores/entry | ScoreEntry | teacher | 成绩录入 |
| /scores/statistics | ScoreStatistics | admin, teacher | 成绩统计 |
| /users | UserList | admin | 用户管理 |
| /announcements | AnnouncementList | admin | 公告管理 |
| /profile | Profile | 所有 | 个人中心 |

---

## 五、技术栈汇总

| 层次 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 (Composition API) | SFC + `<script setup>` |
| UI 组件库 | Element Plus | 表格、表单、菜单等 |
| 状态管理 | Pinia | 用户状态、应用全局状态 |
| 路由 | Vue Router 4 | 路由守卫 + 权限控制 |
| HTTP 请求 | Axios | 请求/响应拦截器 |
| 后端框架 | Express | RESTful API |
| 认证 | JWT (jsonwebtoken) | Token 认证 |
| 密码加密 | bcryptjs | 密码哈希存储 |
| 数据库 | PostgreSQL (Supabase) | 关系型数据库 |
| 数据库驱动 | pg (node-postgres) | Node.js PostgreSQL 客户端 |

---

## 六、实施步骤（共 12 步）

| 步骤 | 内容 | 说明 |
|------|------|------|
| 1 | 项目初始化 | 初始化 server + client 项目，安装依赖 |
| 2 | 数据库建表 | 编写 init.sql，配置 Supabase 连接 |
| 3 | 后端工具层 | 统一响应格式、JWT 工具、学号生成器 |
| 4 | 后端认证模块 | 登录 API、JWT 中间件、角色中间件 |
| 5 | 后端基础业务 API | 学院/班级/学生/教师/课程 CRUD |
| 6 | 后端排课与选课 API | 排课（含冲突检测）、选课、必修自动分配 |
| 7 | 后端成绩与导出 API | 成绩 CRUD、数据导入导出（Excel） |
| 8 | 后端学籍与公告 API | 学籍变更记录、公告管理 |
| 9 | 前端项目骨架 | Layout、路由配置、Axios 封装、Pinia store |
| 10 | 前端登录与权限 | 登录页、路由守卫、动态菜单 |
| 11 | 前端业务页面 | 学院/班级/学生/教师/课程/排课/选课页面 |
| 12 | 前端成绩/系统/公告页面 | 成绩管理、用户管理、公告、个人中心 |
